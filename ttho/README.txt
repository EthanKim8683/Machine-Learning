ECHO + test-time training = model that's adaptive and learns in few examples

i think i should try doing ECHO on an OOD task with a model already trained with
ECHO first, though, to validate my thinking