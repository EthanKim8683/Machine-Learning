i'm calling this one "meta hybrid optimization". basically it's rl except the
reward is how well the model trained (with echo) on the trajectory knows the
world after. basically training the model to self-learn