defaults:
  - _self_
  - preset: null
  
model:
  name: "Qwen/Qwen2.5-Coder-7B-Instruct"

# dataset should have columns `messages` and `resolved`
dataset:
  path: "SWE-bench/SWE-smith-trajectories"
  split: "xml"

trainer:
  output_dir: "trainer_outputs"
  bf16: true
  gradient_checkpointing: true