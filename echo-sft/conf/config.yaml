defaults:
  - _self_
  - preset: null

hydra:
  job:
    chdir: false

# model should use ChatML format
model:
  pretrained_model_name_or_path: ???
tokenizer:
  pretrained_model_name_or_path: ${model.pretrained_model_name_or_path}

# dataset should have columns `messages` and `resolved`
dataset:
  path: ???
  split: ???

data_collator: {}

trainer:
  output_dir: "trainer_outputs"